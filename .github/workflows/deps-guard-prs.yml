name: deps-guard-prs
on:
  pull_request:
    types: [opened, edited, synchronize, reopened, ready_for_review]

permissions:
  pull-requests: read
  issues: read

jobs:
  gate:
    runs-on: ubuntu-latest
    steps:
      - name: Block PR if it closes blocked issues
        uses: actions/github-script@v7
        with:
          script: |
            const owner = context.repo.owner, repo = context.repo.repo;
            const body = context.payload.pull_request.body || '';
            // GitHub's closing keywords (close(s)/fix(es)/resolve(s) #123). :contentReference[oaicite:14]{index=14}
            const re = /(close[sd]?|fix(e[sd])?|resolve[sd]?)\s+#\d+/gi;
            const matches = [...body.matchAll(re)];
            const issueNumbers = [...new Set(matches.map(m => parseInt(m[0].match(/#(\d+)/)[1],10)))];

            const blocked = [];
            for (const n of issueNumbers) {
              const { data } = await github.request('GET /repos/{owner}/{repo}/issues/{issue_number}/dependencies/blocked_by', { owner, repo, issue_number: n });
              const openBlockers = (data||[]).filter(i => i.state === 'open').map(i => i.number);
              if (openBlockers.length) blocked.push({issue:n, blockers:openBlockers});
            }

            if (blocked.length) {
              core.setFailed(
                'PR attempts to close blocked issues:\n' +
                blocked.map(b => `- #${b.issue} is blocked by #${b.blockers.join(', #')}`).join('\n')
              );
            }
